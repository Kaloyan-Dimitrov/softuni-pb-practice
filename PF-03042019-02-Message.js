function encrypter(inputs) {
    inptus = inputs.slice(1);
    inputs.forEach(i => {
        const tagS = i.indexOf('@') == -1 ? i.indexOf('*')
        const letters = i.slice(i.indexOf('@')).split('');
        
    });
}